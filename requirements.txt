# Verified working set for this pipeline (CPU-only sandbox, Python 3.11).
#
# torch/torchvision come from the CPU wheel index:
#   pip install torch==2.9.1 torchvision==0.24.1 --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt

ultralytics==8.4.106      # ships YOLOE + TrackTrack (CVPR 2025)
supervision==0.29.1       # annotators, LineZone counting, VideoSink
opencv-python==5.0.0.93   # OpenCV 5
lap                       # linear assignment used by the trackers

# pulled in automatically by ultralytics on first `get_text_pe` call:
#   clip (git+https://github.com/ultralytics/CLIP.git), ftfy, regex
# and the MobileCLIP text encoder weights `mobileclip_blt.ts` (~572 MB)
